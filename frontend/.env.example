# Laravel backend base URL (no trailing slash, no /api suffix — see src/api/client.ts).
VITE_API_BASE_URL=http://localhost:8000

# Dev server port (see vite.config.ts). Keep in sync with backend/.env's
# FRONTEND_URL / SANCTUM_STATEFUL_DOMAINS and config/cors.php if changed.
MEDINV_PortWeb=5173
